apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: helloworld
  namespace: istio-system
spec:
  hosts:
  - helloworld.fperod.internal
  gateways:
  - ingressgateway
  http:
  - match:
    - uri:
        prefix: /hello
    route:
    - destination:
        host: helloworld-stable.helloworld.svc.cluster.local
        port:
          number: 5000
